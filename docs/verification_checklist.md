# 완료 전 검증 체크리스트 (검증 에이전트용)

작성일: 2026-09-02. CLAUDE.md 9절 종료 루틴의 "검증 에이전트" 단계가 이 문서를 기준으로 판정한다.
원칙: **테스트 통과 ≠ 완료.** 플레이어가 보는 화면과 상태를 실제 창에서 확인한 뒤에만 완료라고 보고한다.

## 0. 실행할 것 (전부 프로젝트 루트, 창이 뜨는 명령 포함)

```powershell
& $env:GODOT_BIN --headless --path . --import
python tools/data/build_resources.py
python tools/art/validate_assets.py
& $env:GODOT_BIN --headless --path . -s -d --remote-debug tcp://127.0.0.1:0 res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://test
& $env:GODOT_BIN --path . -s res://test/tools/playthrough_check.gd -- <출력 폴더>
```

`playthrough_check.gd` 는 시드를 고정하고 이틀을 실제로 진행하며 `playthrough_report.json` 과 단계별 PNG(01~08)를 남긴다. 종료 코드 1 이면 실패 항목이 있다.

## 1. 리포트로 판정 (자동)

`playthrough_report.json` 의 `failed` 가 0 이어야 한다. 항목: 튜토리얼 대화창 / 집이 화면 안 / 카드 수 = 하숙생 + 손님 / 객실 건설 / 배치 / 낮에 일터 도착 / 저녁 정산 / 심사 카드 → 받기 → 손님 카드·액터 표시 / 밤 사연 / 2일차 진입.

## 2. PNG 로 판정 (사람 눈 대신 에이전트가 본다)

| 파일 | 반드시 보여야 하는 것 | 보이면 안 되는 것 |
|---|---|---|
| 01_morning_tutorial | 성주 영감 대화창(초상·이름·대사·다음 버튼) | 깨진 글자, 빈 초상 |
| 02_morning_plain | 왼쪽 위 시계 카드("06:00 아침 · 봄 1일" / "<날씨> · 음기 ●○○ 꼴 · 하숙집" + 진행 바), 오른쪽 위 상태 칩 3개(돈·평판·침대)와 버튼 "취침 [Z]"·"메뉴 [Tab]", 위 가운데 안내 줄, 왼쪽 아래 체력 바, 집 1층 4칸 + 잠긴 층 점선, 객실 앞 요괴 2명, 아래 배치 패널(제목 줄 + 카드 2장 + 휴식·텃밭·동행 줄) | 회색 빈 화면, 집이 잘림, 카드 없음, 요소가 겹침 |
| 03_morning_assigned | 카드 상태 "주방 · 컨디션 …", 메시지 로그 "어둑이 → 주방", 새 객실(RO) 4번째 칸 | 카드가 "휴식" 그대로 |
| 04_day_working | 어둑이가 주방 칸 앞에 서 있음, 진행 바 | 요괴가 객실에 그대로 |
| 05_evening_intake | 심사 카드(문돌이 문구, 종족 스프라이트·첫마디·액운, 받기/거절) | 카드 뒤 글자가 비침 |
| 06_evening_after_accept | 패널에 손님 카드("손님 · …", 드래그 불가 안내), 집에 손님 액터 추가, 상단 침대 n/4 증가, 카드 줄 아래에 휴식·텃밭·동행 드롭존 세 개가 전부 화면 안 | 카드 수가 그대로, 드롭존이 오른쪽으로 밀려 잘림 |
| 07_night_story | 하숙생 대화창, 밤 색조(어두움), 어둑이 주변 빛 | 낮과 같은 밝기 |
| 08_day2_morning | "2일차", 카드 수 = 하숙생 + 손님, 집이 화면 안 | |
| 09_yard_plain (P1-S2) | 상단 바에 "마당", 하늘색 배경 + 초록 바닥선, 문 자리표시(집·우물·뒷산), 텃밭 6칸(테두리로 칸이 구분되는 갈색 사각), 플레이어 스프라이트(PL), 배치 패널 없음, 상호작용 문구는 오른쪽 아래 | 집 단면이 겹쳐 보임, 패널이 남아 있음, 문구가 메시지 로그와 겹침 |
| 10_yard_farmed | 텃밭 0번 칸이 어두운 흙색 + 새싹, 메시지 로그에 "밭을 갈았다 / 무 씨앗을 심었다 / 물을 줬다" | |
| 11_back_hill | 상단 바에 "뒷산", 단차 있는 바닥(경사로), 화면 안 채집 포인트 원 4개(나머지는 오른쪽 화면 밖) + 캔 자리 X 1개, 메시지 "… +1" | 바닥이 평평하기만 함, 포인트 없음 |
| 12_fishing_bar (P1-S3) | 상단 바에 "개울", 바닥에 물 자리표시(청회색 가로 사각), 화면 아래 가운데 타이밍 바(어두운 트랙 + 초록 성공 구간 + 밝은 마커), 프롬프트 "E: 지금!", 메시지 "찌를 던졌다…" | 바가 없음, 프롬프트가 "E: 낚시" 그대로 |
| 14_inventory_menu | 장부 메뉴 창고 탭: 제목 "장부", 탭 버튼 3개(창고 [I] 눌림·하숙부 [J]·명부 [L]), 종류별 소제목(요리·음식 / 재료 / 수확물 …) 아래 "이름 ×n   개당 값" 행, 스크롤바, 닫기. 상단 칩에 돈 8750·평판 33 | 빈 패널, 닫기가 화면 밖 |
| 15_ash_field (P1-S4) | 상단 바에 "잿빛 들", 회보라 하늘·어두운 바닥, 플레이어 곁에 동료 스프라이트 2개(TT·ED, 머리 위 청록 체력 바), 보라 사각 적 여러 마리(머리 위 주황 체력 바), 로그 "잿빛 들. 동료 2명 동행. Q 투척 부적 …" | 적·동료가 없음, 마당 배경색 그대로 |
| 16_talisman_flight | 주황 마름모(투척 부적)가 플레이어 앞에 날아가고 있거나 적 체력 바가 줄었음, 로그 "투척 부적!" | 부적이 안 보이고 로그도 없음 |
| 17_calendar_tab (P2-S1) | 장부 메뉴 달력 탭(탭 버튼 4개, 달력 [K] 눌림): 제목 "봄 — 2/28일 (통산 2일차)", 7열 × 4줄 날짜 격자(오늘 2 가 주황 ●, 10·11·20·21 에 ◆, 1 은 흐림), "다가오는 것 · 범례(● 오늘 ◆ 소절기 ★ 명절)" 한 줄, 그 아래 "10일 — 장마 시작 (2일간)"·"20일 — 만월 (2일간)" 두 줄이 스크롤 없이 전부 보임, 닫기. 패널 위쪽이 시계 카드 아래(겹침 없음) | 격자 없음, 오늘 표식 없음, 만월 줄이 잘림, 패널이 시계 카드를 덮음 |
| 13_cook_menu | 패널 제목 "조왕 부엌 가마솥 — 요리", 첫 줄 "주방 · 개조" 버튼, "가마솥이 비어 있다", 티어 1 레시피 6줄(재료·초 + 요리 버튼), 오른쪽 스크롤바(티어 2 미해금 6줄은 접힘 아래), 닫기 버튼이 패널 안에 온전히 | 빈 패널, 닫기가 화면 밖, 집 단면이 가려지지 않고 겹침 |

## 3. 판정 기록

에이전트는 각 항목을 PASS / FAIL / 판단불가 로 적고, FAIL 은 파일명과 관찰 내용을 함께 적는다. FAIL 이 하나라도 있으면 완료 보고를 하지 않고 수정으로 돌아간다.
"판단불가"(예: 소리, 조작감)는 사용자에게 명시적으로 넘긴다.
